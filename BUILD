# BUILD at the root level

# Optionally, define other global rules or targets here

# Load Gazelle rule
load("@gazelle//:def.bzl", "gazelle")

# Define the Gazelle target
gazelle(
    name = "gazelle",
    # You can specify additional Gazelle options here if needed
)
